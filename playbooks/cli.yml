---
- name: CLI tools setup
  hosts: localhost
  connection: local
  gather_facts: true
  become: false

  tasks:
    - name: Install CLI tools
      ansible.builtin.include_role:
        name: cli_tools
