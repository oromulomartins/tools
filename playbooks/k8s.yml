---
- name: Kubernetes tools setup
  hosts: localhost
  connection: local
  gather_facts: true
  become: false

  tasks:
    - name: Install Kubernetes tools
      ansible.builtin.include_role:
        name: kubernetes
